<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0"
      crossorigin="anonymous"
    />
    <style>
      .teaser {
        text-overflow: ellipsis;
        overflow: hidden;
        white-space: nowrap;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div>
        <h1 class="mb-4">Blogs</h1>
      </div>
      <a href="/new" class="btn btn-success">New Post</a>
      <% blogs.forEach(blog=>{%>
      <div class="card mt-2 mb-2">
        <div class="card-body">
          <div class="card-title"><%= blog.title%></div>
          <div class="card-subtitle">
            <%= blog.createdAt.toLocaleDateString()%>
          </div>
          <div class="card-text mb-2 teaser"><%=blog.content%></div>
          <a href="show/<%= blog.id %>" class="btn btn-primary">Read More</a>
          <a href="/edit/<%= blog.id %>" class="btn btn-info">Edit</a>
          <form
            action="/delete/<%= blog.id %>?_method=DELETE"
            method="POST"
            class="d-inline"
          >
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
      <% }) %>
    </div>
  </body>
</html>
